import { createTheme } from '@mui/material/styles';
import { cyan, yellow, pink } from '@mui/material/colors';

const sellerTheme = createTheme({
  palette: {
    primary: pink,
    secondary: yellow,
  },
});

export default sellerTheme;
